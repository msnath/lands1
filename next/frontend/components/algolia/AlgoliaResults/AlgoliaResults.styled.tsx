import { Colors } from "@/styles/app.styled";
import styled from "styled-components";

const Text = styled.p`
  color: ${Colors.Black101010};
  font-family: Mulish;
  font-size: 1.125rem;
  font-weight: 700;
  text-align: left;
  margin: 1rem 0rem;
`;

const S = { Text };

export default S;
