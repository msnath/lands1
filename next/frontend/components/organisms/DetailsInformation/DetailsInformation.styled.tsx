import { padding } from "@/styles/app.styled";
import styled from "styled-components";

const Container = styled.div`
  display: grid;
  row-gap: 1rem;
  ${padding.hori};
`;

const S = { Container };

export default S;
